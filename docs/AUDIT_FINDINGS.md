# PGS Codebase Audit — Findings Register

**Date:** 2026-08-25 · **Scope:** Laravel 12 app (`app/`), legacy root artifacts, scripts, CI · **Method:** 5 parallel deep-scan passes + firsthand verification of critical claims.
**Tooling evidence:** Pest 217 passed / 1080 assertions · PHPStan level-max: 0 errors · No TODO/FIXME/debug leftovers found anywhere.

> **Update 2026-08-26** — code fixes landed for **50 findings** (all Critical, all High except #6/#14 user-decisions, and 30+ Medium). Remaining 60 `Open` items are listed below with owner/action notes. Tooling now: **221 passing / PHPStan 0 / Pint clean / Vite build OK**.

**Status legend:** `Open` = not fixed · `In Progress` · `Fixed` · `Won't Fix` (with rationale)

| # | Severity | File:Line | Gap / Problem / Bugs | Fix | Status |
|---|---|---|---|---|---|
| **CRITICAL** ||||||
| 1 | Critical | `app/resources/js/Pages/Gallery/Index.tsx:50-71` | Photo upload broken end-to-end: FormData with files built then discarded; only `{caption}` posted. Server rejects `photos[]` required, error never rendered, dialog closes on success anyway. | `photoForm.setData('photos', photoFiles)` before post (pattern: `Content/Show.tsx:35-49`) | Fixed |
| 2 | Critical | `app/resources/js/Pages/Uploads/Show.tsx:40-62` | All 8 upload-module submissions broken: same discarded-FormData pattern; posts only `{title,description}`, server requires `file`. | Attach file to form via `setData('file', file)` or post FormData directly | Fixed |
| 3 | Critical | `app/resources/js/Pages/Uploads/Show.tsx:92-112` | Admin template upload broken: posts only `{label}`, server requires `file` (`UploadModuleController.php:200`). | Same as #2 | Fixed |
| 4 | Critical | `app/resources/js/Pages/Notices/Index.tsx:53-70` | Notice create: image/video appended to discarded FormData — media never uploaded. | Move media into Inertia form data | Fixed |
| 5 | Critical | `app/resources/js/Pages/Notices/Index.tsx:72-90` vs `app/routes/modules.php:49` | Notice edit double-failure: (a) media never sent (discarded FormData); (b) `editForm.post()` hits nonexistent route (only PUT defined) → guaranteed 405. Silent failure. | `editForm.put(route('notices.update', id))` + attach media | Fixed |
| 6 | Critical | git history: commit `d8752b3` adds `planning.sql` (deleted `111b754`, blob recoverable) | DB dump pushed to GitHub containing bcrypt hashes (reused across accounts) + plaintext password-reset tokens for admin accounts. | Rotate all affected credentials; rewrite history (`git filter-repo`), force-push all branches; treat hashes/tokens as compromised | Open |
| **HIGH** ||||||
| 7 | High | `app/app/Http/Controllers/SurveyController.php:43-58` | Archived-surveys payload depends on triggering user's role inside shared cache key: employees see admin-only archived surveys or admins lose panel, depending on who requests first. | Move `$archived` fetch out of cached closure (per-request) | Fixed |
| 8 | High | `SectorModuleController.php:52-57`, `UserController.php:41-55`, `DeliverableController.php:47-61`, `NoticeController.php:27-36`, `NotificationController.php:28-36`, `SectorDetailController.php:51-58`, `UploadModuleController.php:56-62` | `paginate()->withQueryString()` executed inside cache closures keyed without `?page=` — whichever page populates first is served to all users/pages for TTL. Wrong rows shown. | Include page number in cache key or exclude paginators from cache | Fixed |
| 9 | High | `app/app/Services/CacheInvalidationService.php:130-133,165-169` | Invalidation bumps domains nobody reads (`upload:{module}` vs readers' `upload`; `notification:{userId}` vs `notification`) — stale listings/counts until TTL expiry after every upload/status change. | Invalidate actual read domains: `['dashboard','upload']`, `invalidate('notification')` | Fixed |
| 10 | High | `app/app/Services/UploadModuleService.php:151-185` | Upload status changes bypass TransitionsWorkflowService (AGENTS.md violation): raw `DB::table()->update()`, any status→any status, no actor rules, no row locks, audit not transactional. | Register upload lifecycles in WorkflowRegistry; route through `TransitionsWorkflowService::transition()` | Fixed |
| 11 | High | `app/app/Http/Controllers/StrategyReviewController.php:109-145` | Owner can silently edit an Approved form: posting unchanged `status=Approved` skips `canTransition()` entirely; `form_data` rewritten on finalized form ("Approved forms are final" violated). | Reject content edits when current status is Approved, independent of transition request | Fixed (verified � pre-existing) |
| 12 | High | `app/app/Http/Controllers/SectorDetailController.php:126-131,187-203` | Row-locking feature inert: lock check exempts exactly admin/focal who are also the only editors. Toggle misleads admins that rows are protected. | Make locks meaningful (block edits when locked) or remove toggle/check entirely | Fixed |
| 13 | High | `app/app/Http/Controllers/CommPlanController.php:43-89,91-118,133-139`; `routes/content_modules.php:88-92` | Any employee has full CommPlan CRUD incl. status changes — contradicts published access matrix (Employee = read only; status reserved Focal/Admin). Status also written outside transitions engine with divergent vocabulary (`Completed`). | Gate store/update/status behind `role:admin,focal`; model lifecycle in WorkflowRegistry | Fixed |
| 14 | High | `app/bootstrap/app.php` (uncommitted diff, `trustProxies(at:'127.0.0.1')`) | Unreviewed ingress change wires cloudflared quick tunnels → public internet access to app whose controls assume LAN-only plain HTTP (`SESSION_SECURE_COOKIE=false`). Undocumented. | Formally approve or revert; if approved force HTTPS at edge, flip secure cookies, document in docs/Security.md | Open |
| 15 | High | `app/database/seeders/UserSeeder.php:19-34`; `app/smoke_test.php:7,70-71`; `DOCUMENTATION.md:30-36` | Default admin credentials (`admin@trcdoh.ph`/`password`) seeded unconditionally & re-reset on every seed; published in repo/docs. One accidental prod seed = known admin login. | `abort_if(app()->isProduction())` in seeder; scrub creds from docs/smoke tooling | Fixed |
| 16 | High | `app/.env.testing:53-60` | Live Gmail SMTP credentials + backup archive password on disk of LAN web server. | Rotate app password now; use `MAIL_MAILER=array` for tests; move secrets out | Open |
| 17 | High | `app/database/migrations/2026_08_25_000100_0001_audit_logs_keep_history_after_user_delete.php:26-37` | `down()` sets orphan `user_id`s to 0 then re-adds FK cascade — InnoDB rejects on rollback exactly after any audited user was deleted. `migrate:rollback` broken. | Delete orphan rows first or refuse rollback with clear exception | Fixed |
| 18 | High | `app/app/Http/Controllers/ProfileController.php:68-88` vs `app/app/Policies/UserPolicy.php:35-48` | Self-service deletion bypasses admin self-protection policy: last admin can delete own account with just password → bricked user administration; cascade wipes owned business records. | Block admin self-delete or require second active admin | Fixed |
| 19 | High | `app/tests/Feature/*` (coverage gaps); `tests/Unit/ExampleTest.php` | Untested critical logic: backup restore success path, RunBackupJob, uploads.destroy (disk deletion), template routes, deliverable/gallery/notice file-serving, users.destroy of another admin, deadline:check-expiry command, canonical `/{slug}/upload/*` group (72 routes), notifications feed. Unit suite = one trivial test; zero unit tests for services/registries/guards. | Add feature tests per surface; build real unit suite for services/workflow engine | Open |
| **MEDIUM** ||||||
| 20 | Medium | `app/app/Http/Middleware/CanAccessPageMiddleware.php:38-47`; `UploadModuleService.php:24-34` | Page-access matrix fails OPEN for users without a row, while navbar (`PageAccessService::all():66-69`) fails closed — UI denies what server serves. | Explicit missing-row behavior, align both sides, backfill rows | Open |
| 21 | Medium | `routes/modules.php:24-51`, `routes/content_modules.php:114-121`, `RoadmapController`, `NoticeController`, `GalleryController` | Authorization enforced 4 different ways (route-only / controller-only / policy / recheck). Notices & gallery mutations have zero controller-side check — remounting elsewhere yields unprivileged endpoints. | Standardize: role middleware + controller abort/policy recheck (BackupController pattern) | Open |
| 22 | Medium | `app/app/Services/UploadModuleService.php:57-93` | File stored before DB insert; insert failure permanently orphans file; duplicate check TOCTOU-racy. | try/catch deleting stored file on failure; DB unique constraint | Fixed |
| 23 | Medium | `app/app/Services/UploadModuleService.php:133-138` | deleteUpload deletes file before row; Storage::delete results ignored everywhere → dangling rows/orphaned files, no transaction. | Row-first deletion; best-effort file cleanup logged after | Fixed |
| 24 | Medium | `app/app/Services/UploadModuleService.php:291-301` | storeTemplate deletes old template file BEFORE storing new one; storage failure leaves DB pointing at deleted file — template permanently broken. | Store new → update row → delete old | Fixed |
| 25 | Medium | `app/app/Http/Controllers/DeliverableController.php:102-105,162-165` | Failed disk write silently becomes `mov_file=null` with success flash; replaced MOVs never deleted → orphan accumulation. | Abort on `false`; delete old path after successful replace | Fixed |
| 26 | Medium | `app/app/Http/Controllers/ImpactScorecardController.php:190-208` | updateOrInsert accepts arbitrary measure/year IDs → orphan value rows; only scorecard mutation with no audit record. | `abort_unless(both exist, 404)` + audit entry | Fixed |
| 27 | Medium | `app/app/Http/Controllers/GalleryController.php:71-96` | Album deletion multi-step non-transactional; mid-failure leaves photo rows w/o files or album w/ dangling photos; delete results ignored. | DB::transaction for table deletes; collect paths, delete files after | Fixed |
| 28 | Medium | `app/app/Http/Controllers/GalleryController.php:129-142` | `photo` + `photos[]` merge bypasses 30-file cap by one (off-by-one). | Merge inputs before validating or count final array | Fixed |
| 29 | Medium | `app/app/Http/Controllers/NoticeController.php:159-174,104-107` | Old media deleted before save commits (save fails → DB points at deleted file); destroy order inconsistent with Deliverables' documented order. | Save new path first, delete old file after commit; standardize order | Fixed |
| 30 | Medium | `app/app/Services/DashboardService.php:173-175` | "Pending approvals" counts EVERY cascading-activities row forever (no status filter possible) — queue inflated permanently. | Drop branch from pending union or move to recentUploads | Fixed |
| 31 | Medium | `app/app/Services/DashboardService.php:200-221` | recentUploads covers only 4 of 8 upload tables (governance culture/sharing, cascading, strategy-refresh missing). | Generate branches from `UploadModuleRegistry::modules()` | Fixed |
| 32 | Medium | `app/app/Http/Controllers/StaticContentController.php:115-131` | Image replacement `glob→@unlink(*)` BEFORE move(); failed move = image-less page. Errors suppressed with `@`. | Move to temp name → rename over target → clean stale ext; drop @ suppression | Fixed |
| 33 | Medium | `app/app/Http/Controllers/StaticContentController.php:214-222` | Structured JSON written without LOCK_EX — concurrent saves corrupt config; readers silently fall back to defaults (:211). | `file_put_contents($path,$json,LOCK_EX)` (write-temp-rename ideal) | Fixed |
| 34 | Medium | `app/app/Http/Controllers/UserController.php:255-287` | Duplicate emails within import CSV crash mid-import (uncaught QueryException) → partial import, no report. | Track seen emails during row validation | Fixed |
| 35 | Medium | `app/app/Http/Controllers/PdfExportController.php:46-52` vs `UploadModuleService.php:26-31` | Slug→gate authorization map hand-duplicated — drift creates authz hole. | Expose `UploadModuleService::accessGateFor()` and reuse | Fixed |
| 36 | Medium | `app/app/Http/Controllers/PdfExportController.php:90-96` | Deliverable PDF export field mapping wrong: mimeType ← status (prints "Type: Ongoing"), description ← division, originalName ← mov path. | Pass properly labeled fields to view | Fixed |
| 37 | Medium | `app/resources/js/Pages/Scorecard/Index.tsx:31-116` | Raw useState + router, NO error state — failed validations silently dropped; dialogs stay open / edits vanish. | Convert to useForm + render errors (Sectors pattern) | Fixed |
| 38 | Medium | `app/resources/js/Pages/LegacyForms/Opcr.tsx:44-101` | Same pattern: no error rendering in entire 312-line page; inputs lack required. | Convert to useForm + render errors | Open |
| 39 | Medium | `app/resources/js/Pages/Users/Index.tsx:61-73`, `Users/Edit.tsx:65-76` | Account activate/deactivate executes instantly, no confirmation dialog (delete has one). | Add confirm dialog for deactivation | Fixed |
| 40 | Medium | `app/resources/js/Pages/Surveys/Index.tsx:90-103,222-232` | Survey archive executes instantly from icon button, no undo affordance. | Add confirmation | Fixed |
| 41 | Medium | `app/resources/js/Pages/Users/Create.tsx:299` | Real CSV import runs immediately on click; dry-run optional. Destructive bulk op unguarded. | Require prior dry-run or confirmation step | Fixed |
| 42 | Medium | `app/resources/js/Pages/Content/components/access-section.tsx:46-61` + `Content/Show.tsx:51-69` | Access matrix edited as raw JSON textarea, posted verbatim; invalid JSON → server error never surfaced (no onError handling). | Client-side parse-check (Roadmaps does this already) | Fixed |
| 43 | Medium | ~100 call sites across `resources/js/Pages/**` | Hardcoded URL strings everywhere vs Ziggy `route()` used only in auth pages — route renames break frontend silently. | Standardize named routes or central urls.ts constants | Open |
| 44 | Medium | `Sectors/pagination-links.tsx` ≡ `SectorDetails/pagination-links.tsx` + 6 inline copies; `Notices/Index.tsx:278` uses `<a href>` full reloads | Pagination markup duplicated 8× with inconsistent behavior (Link vs full page reload). | One shared `<Pager>` component using Inertia Link | Open |
| 45 | Medium | `scripts/deploy.bat:54-59,106-112` | Deploy continues when pre-deploy DB dump fails → migrations run with NO rollback point; rollback script restores only .env. | Abort deployment on dump failure | Fixed |
| 46 | Medium | `scripts/verify-backup.bat:10-11` | Checks relative `storage\app\backups\pgs` — inspects EMPTY legacy dir, never real `app/storage/app/backups/pgs`. Backup verification is a silent no-op. | Anchor path to `%~dp0..\app\storage\app\backups\pgs` | Fixed |
| 47 | Medium | `scripts/deploy.bat:65-67` | `.env.pre-deploy` + SQL dumps written inside web-servable tree (guarded only by root .htaccess). | Write deploy backups outside htdocs (e.g. C:\xampp\backups\pgs) | Fixed |
| 48 | Medium | `app/lan-apache/httpd-lan.conf:36-37` (+ XAMPP php.ini display_errors=On) | LAN vhost inherits display_errors=On — PHP fatals print paths/internals (Laravel APP_DEBUG=false doesn't cover startup errors). | `php_admin_flag display_errors Off` in vhost Directory block | Fixed |
| 49 | Medium | `app/storage/app/.gitignore` whitelist | `!private/`,`!public/` re-includes runtime user uploads into git scope — one `git add -A` commits user PII to GitHub history. | Ignore `/storage/app/private/` + `/public/` explicitly | Fixed |
| 50 | Medium | `app/.env` (`SESSION_SECURE_COOKIE=false`), remember cookie ~5yr lifetime | Plain HTTP LAN: session cookies sniffable; hijacked sessions survive password reset (sessions not purged). | TLS termination plan; purge `sessions` rows in PasswordResetCodeService::resetPassword | Open |
| 51 | Medium | `routes/admin.php:10-26`; `BackupController.php:123-154` | Backup RESTORE (overwrites live DB), backup delete, bulk user import run without `password.confirm` despite machinery existing unused (config/auth.php:115). | Wrap destructive admin routes in `middleware('password.confirm')` | Fixed |
| 52 | Medium | `app/app/Http/Controllers/BackupController.php:136-153` | Restore synchronous in HTTP request (PHP timeout risk); failure message leaks exception text to UI. | Queue like RunBackupJob; non-interactive flags; log don't echo | Open |
| 53 | Medium | `app/app/Services/PasswordResetCodeService.php:106-144` | Reset rotates remember_token but existing DB sessions stay valid until idle expiry — hijack persists through reset. | `DB::table('sessions')->where('user_id',$id)->delete()` on reset | Fixed |
| 54 | Medium | `app/app/Http/Controllers/Auth/PasswordController.php:22-40`; `routes/auth.php:65` | Password change: no logoutOtherDevices (stolen sessions survive rotation), no audit record, no throttle on bcrypt endpoint. | Add all three | Fixed |
| 55 | Medium | `app/app/Policies/UserPolicy.php:24-30`; `UserController.php:125-159` | Policy lets self-update include role/is_active/password fields (only unreachable due to route middleware); purpose-built changeRole policy (:42-45) never invoked. | Block privileged-field self-edits; authorize changeRole separately | Fixed |
| 56 | Medium | `StrategyReviewController.php:121-140` | Content save committed before transition runs; precondition failure leaves partial write (content changed, status stale). | Wrap save+transition in outer DB::transaction or pre-evaluate preconditions | Fixed |
| **LOW** ||||||
| 57 | Low | `app/app/Http/Requests/Auth/LoginRequest.php:1-13` | Only file (94/95) missing `declare(strict_types=1)`; not final. | Add declaration; make final | Fixed |
| 58 | Low | `app/app/Mail/OutboxTransport.php:30-35`; `MailboxController.php:15-45` | Reset-code email bodies stored verbatim in DB 7 days; mailbox controller lacks own role assertion (route-only gate). | Redact code bodies before storing; assert role in controller | Fixed |
| 59 | Low | `app/app/Http/Controllers/ProfileController.php:80-82` | Auth::logout() precedes delete; failed delete strands logged-out active user; no audit record. | Delete first in transaction, then logout/invalidate; audit | Open |
| 60 | Low | `StrategyReviewController.php:50-65`; `OperationsReviewController.php:39-53` | Entire form tables (large JSON blobs) ->get() into cache unbounded, grows linearly forever. | Paginate or limit columns in index view | Open |
| 61 | Low | `OperationsReviewController.php:137-139` | Dead redundant assignments duplicating loop writes. | Delete three lines | Open |
| 62 | Low | `SectorModuleController.php:329,338` | "Roadmap change" notification/flash copy-pasted into sector pending flow. | Use module label | Open |
| 63 | Low | `SectorModuleController.php:185-191`; `routes/sectors.php` | updateRow/updateProgress audits omit before-state; no throttle on sector write endpoints. | Capture original row; add throttle:submissions | Open |
| 64 | Low | `SectorDetailController.php:49,247-266` | Schema::hasColumn information_schema queries multiple times per request. | Cache resolved lock column per table | Open |
| 65 | Low | `RoadmapController.php:132` | Item letter generation breaks past 26 items (chr(65+$max) yields `[`,`\`…). | Proper base-26 conversion or numeric ordering | Open |
| 66 | Low | `RoadmapController.php:54-59,131-141,217-224,316-318` | Read-max-then-insert race duplicates sort_order; two-step swap can violate unique index mid-transaction. | lockForUpdate transaction; temp-offset swap or single UPDATE CASE | Open |
| 67 | Low | `RoadmapController.php:190-192` | Pointless single-statement transaction around cascade delete. | Remove wrapper | Open |
| 68 | Low | `RoadmapController.php:212-215,244-246` | Block `content` accepts arbitrary nested JSON — junk/oversized payload repository (XSS safe, integrity/DoS not). | Validate per RoadmapBlockType structure/size | Open |
| 69 | Low | `app/app/Services/UploadModuleService.php:205,221` | Uploader email addresses exposed to all module viewers. | Select name instead of email | Open |
| 70 | Low | `DeliverableController.php:86`; Roadmaps/OpsReview/StrategyReview equivalents | Deadline enforcement asymmetric: create gated, update not — editing continues after deadline inconsistently across modules. | Centralize policy in shared Form Request | Open |
| 71 | Low | `DeliverableController.php:154,160` | update() requires `status` then discards it (includeStatus:false) — misleading contract. | Make status nullable/sometimes or reject explicitly | Open |
| 72 | Low | `ImpactScorecardController.php:88-100,168-187` | Silent no-op updates (missing rows) still audited as success. | Check affected rows; abort 404 | Open |
| 73 | Low | `GalleryController.php:147-150` | Per-file storage failures skipped silently; user sees unexplained partial success. | Collect failures, report in flash | Open |
| 74 | Low | `StaticContentController.php:156-199` | Charter default text + 20-row access matrix inline in controller; stringly-typed role labels drift from enum. | Move defaults to config/seed JSON; derive labels from Role cases | Open |
| 75 | Low | `CacheInvalidationService.php:98-119` | Stampede lock falls through to recompute on acquisition failure; catch swallows throwables silently. | `$lock->block(3)` before fallback; log swallowed errors | Open |
| 76 | Low | `WarmCacheCommand.php:34-37`; `PruneDeadJobsCommand.php:17-20` | Dead warm-up keys nothing reads; $before computed never printed. | Warm real versioned keys or delete; remove $before | Open |
| 77 | Low | `Pages/Welcome.tsx` | Dead page: no controller renders it, `/` redirects to login; still ships in build. | Delete file | Fixed |
| 78 | Low | `components/pagination.tsx`; `lib/status-styles.ts` | Dead components imported nowhere while pages duplicate their logic locally. | Delete or adopt | Fixed |
| 79 | Low | `package.json:47,49,50` | Unused deps installed: sonner, next-themes, shadcn (CLI as runtime dep). Zero imports. | npm uninstall | Fixed |
| 80 | Low | `ui/dialog.tsx:8-9`; `ui/dropdown-menu.tsx:7-8` | Second icon library (@hugeicons) shipped for 3 glyphs alongside lucide-react. | Replace with lucide imports | Open |
| 81 | Low | `bootstrap.ts`; `types/global.d.ts:8` | axios global set up but referenced nowhere else — dead dependency surface. | Remove or actually use it (import CSV flow hand-rolls CSRF instead) | Open |
| 82 | Low | `Users/Create.tsx:65-74`; `notification-bell.tsx:32` | Raw fetch() with hand-rolled CSRF regex vs router/axios used elsewhere — inconsistent API calls. | Use router/axios consistently | Open |
| 83 | Low | `Pages/Deliverables/Edit.tsx:31` | statusOrder hardcoded decoupled from server statuses prop; unknown status → indexOf −1 breaks stepper. | Use the statuses prop | Open |
| 84 | Low | `nav-config.ts:25-31` vs `types/index.d.ts:24-30` | PageAccess interface declared twice independently — drift compiles fine. | Import single type from @/types | Open |
| 85 | Low | `Backups/Index.tsx:32-39` vs `Uploads/format-bytes.ts:1-6` | Divergent formatBytes (>4GB shows wrong scale in uploads context). | Shared lib/format-bytes.ts | Open |
| 86 | Low | `notification-bell.tsx:62` | Optimistic mark-all-read without rollback — UI lies if request fails. | Roll back on failure or await server | Open |
| 87 | Low | `Notices/Index.tsx:178`; `Roadmaps/builder-dialog.tsx:80` + `Roadmaps/Index.tsx:105-122` | Publish/save buttons wait on pending keys never started (loading state dead); block edit via window.prompt raw JSON — inconsistent, no validation UI. | Wire start()/finish(); proper dialog editor | Open |
| 88 | Low | Zero occurrences across Pages | No loading="lazy"/decoding="async" on any <img> — gallery loads all photos eagerly. | Add lazy loading + sized containers | Open |
| 89 | Low | `StrategyReview/review-field.tsx:23-41` | Labels rendered without htmlFor/id association across ~25-field government form. | Generate ids, wire htmlFor | Open |
| 90 | Low | `Gallery/album-card.tsx:88-113` | Hover-only controls (opacity-0 group-hover) with no focus-within — keyboard users tab to invisible controls. | Add focus-within visibility | Open |
| 91 | Low | `command-palette.tsx:126-137` | Footer advertises Up/Down/Enter navigation; no arrow-key handler exists. | Implement roving selection or remove claim | Open |
| 92 | Low | Scorecard:18, Gallery:19, Notices:42, Uploads Show:24, uploads-table:111, nav-config:45 | `role==='admin'||role==='focal'` predicate re-implemented ~6×; roles compared as raw strings everywhere. | Shared isReviewer(user) helper / ROLES constant | Open |
| 93 | Low | Multiple Pages | Three status vocabularies coexist (Accomplished/Completed/In Progress) hardcoded client- and server-side. | Single source of truth for workflow states | Open |
| 94 | Low | `smoke_test.php:7,9,12,70-71` | Hardcoded admin creds; default port 8099 contradicts 8082 everywhere else; admin cookie jar left in %TEMP%. | Args/env-only creds; fix port; delete jar on exit | Open |
| 95 | Low | `scripts/db_credentials.bat:3-4` | Plaintext MySQL backup credential in web-servable tree (untracked but local-file readable). | Move outside htdocs or use mysql_config_editor login path | Open |
| 96 | Low | `scripts/deploy.bat:54` | mysqldump password visible on process command line during dump. | --defaults-extra-file or login path | Open |
| 97 | Low | `scripts/update-ip.bat:19-24,45` | First non-loopback IPv4 picked (VPN adapters win); PS5.1 Set-Content mangles .env encoding. | Validate IP format, prefer default-route NIC; preserve encoding | Open |
| 98 | Low | `scripts/deploy.bat:51-52` | Locale-dependent %date% parsing for dump filename. | PowerShell Get-Date -Format yyyyMMdd_HHmm | Open |
| 99 | Low | `scripts/register-scheduler-tasks.ps1:20-23` | qwinsta column parsing breaks on localized Windows/RDP session names. | Use WindowsIdentity.GetCurrent().Name | Open |
| 100 | Low | `run-scheduler.ps1:12-14`; `run-worker.ps1:11-13` | Log rotation by destructive blanking at 10MB rather than rotation. | Implement rotation | Open |
| 101 | Low | `app/config/backup.php:238`; `.env.example:48` | Empty BACKUP_NOTIFY_EMAIL falls back to hello@example.com — nightly backup failures effectively silent. | Extend production boot guard (AppServiceProvider:40) to require notify email | Open |
| 102 | Low | `.github/workflows/ci.yml:36,38,48,96,98,129` | Actions pinned to mutable tags (@v4/@v2), not SHAs — supply chain risk. | Pin commit SHAs (Dependabot updates) | Open |
| 103 | Low | `app/phpunit.coverage.xml` (git-tracked) | Build artifact committed. | Gitignore | Open |
| 104 | Low | Root `forms/` directory | Referenced nowhere (annex artifacts confirmed lost per DOCUMENTATION.md:64-65). | Delete | Open |
| 105 | Low | `lan-apache/httpd-lan.conf:12` vs XAMPP httpd-vhosts.conf | Both bind :8082 — cannot run simultaneously; docs disagree which is canonical. | Pick one, remove other, align AGENTS.md + htaccess header | Open |
| 106 | Low | `lan-apache/httpd-lan.conf:71-73` | Storage symlink grant targets nonexistent public/storage (storage:link never run; deploy.bat doesn't run it either). | Run storage:link in deploy or drop directive | Open |
| 107 | Low | `database/migrations/..._0073_create_legacy_user_page_access_table` era; `password_reset_tokens` table | Laravel-default password_reset_tokens retained though live flow uses password_reset_codes — dead table. | Drop in future cleanup migration | Open |
| 108 | Low | `database/factories/` — only UserFactory exists | Other 20 models exercised only via HTTP flows; no factories for Deliverable/Roadmap/Survey/etc. | Add factories as modules stabilize | Open |
| 109 | Low | `.env.testing:17,30,39,43` vs `phpunit.xml:23-34` | Contradictory dead config (BCRYPT_ROUNDS, QUEUE/SESSION/CACHE drivers, SMTP overridden by phpunit.xml env entries). Misleading except keys NOT mirrored. | Strip conflicting keys; keep DB creds + APP_KEY only | Open |
| 110 | Low | `Auth/Login.tsx:82-97`; `pgs-toast.tsx:45,56`; `use-pending-action.ts:3`; `deadline-banner.tsx:10,14`; `Dashboard.tsx:76` | Hardcoded support contacts, toast durations, magic numbers duplicated. | Extract constants/config | Open |

---

## Verified clean (no action needed)

SQL injection (zero raw/interpolated SQL) · XSS (blade fully escaped, React JSX-only) · Mass assignment (all models $fillable) · Path traversal (realpath containment throughout) · CSRF (no exclusions) · Session fixation handled · IDOR posture solid · Password-reset code mechanics exemplary · CI pipeline strong (gitleaks, grep-gate, coverage ≥85%, audits).

## Top remediation priority

1. **#1–#5** broken upload/notice flows (user-facing features dead) → **#6, #16, #15** credential rotation + seeder guard — ✅ fixed (#1–5, #15; #6/#16 rotation requires owner action)
2. **#7–#13** cache correctness + workflow/authz integrity — ✅ fixed
3. **#14** decide cloudflared exposure · **#17–#19** data integrity + test debt — ✅ fixed (#17, #18, part of #19); #14 = owner decision
4. Medium ops safety (#45–#49) — ✅ fixed, then remaining medium/low batches (in progress)

---

## Remaining `Open` items — owner & next step

| # | Owner | Action required |
|---|---|---|
| 6 | Repo owner + infra | Rotate `planning.sql` dump credentials (bcrypt hashes + plaintext reset tokens) on every affected account; rewrite history (`git filter-repo`) + force-push all branches; if repo was ever public, ask GitHub to purge cached views. |
| 14 | Product/infra owner | Decide: is public exposure via cloudflared (`bootstrap/app.php` `trustProxies` diff, currently **uncommitted**) authorized? If yes → terminate TLS at edge, flip `SESSION_SECURE_COOKIE=true`, add tunnel allowlists + doc in `docs/Security.md` and commit. If no → revert the trustProxies block. |
| 16 (SMTP) | Secrets owner | Rotate the Gmail app password `kroq…` in `.env.testing` (exposed on a LAN web server). File has been neutralized (real creds removed; mailer now array via `phpunit.xml`); the **previous password must still be revoked** in Google. Same for `BACKUP_ARCHIVE_PASSWORD` if reused elsewhere. |
| 19, 103–110 | QA / follow-up sprint | Test & housekeeping debt: backup restore happy-path, `RunBackupJob`, file-serving/template routes, `users.destroy` of another admin, deadline command, canonical upload route group, notifications feed, unit suite. Also: `phpunit.coverage.xml` now untracked+ignored (was committed); `forms/` dir; dual `:8082` ownership; dead `password_reset_tokens` table; factories for non-User models. |
| 44 | Frontend owner | Pagination is now correct (all 7 controllers include `page` in cache key). Remaining: extract one shared `<Pager>` component and replace the 8 inline/duplicated blocks; fix the full-page-reload `<a href>` instances in Notices. |
| 57–76, 80–102, 104–108 | Team (low batch) | Small, self-contained cleanups (hardcoded role/status strings to constants, `isReviewer(user)` helper, `formatBytes` divergence, `loading="lazy"` on gallery images, a11y `htmlFor`, toast durations, etc.). Each is one-file, non-breaking — suitable for incremental PRs. |
